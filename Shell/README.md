该简易shell支持的命令：

所有内置基础命令、外部命令、单管道、支持后台运行



能学到的知识：

后台进程管理（包括信号通信signal(SIGCHLD)、后台进程清理）

创建进程fork，并根据返回值确定执行内容

执行外部命令execvp

解析命令（多方面记录，令牌数组、参数数量、后台运行状况与管道情况）

特殊命令：管道命令的解析，实现进程间通信 管道的创建dup与重定向dup2

